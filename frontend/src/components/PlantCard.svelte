<script>
  import { createEventDispatcher } from 'svelte';
  import { fly } from 'svelte/transition';

  // Rimuovi o commenta questa riga se non hai un file types.ts
  // /** @type {import('../../types').Plant} */ 
  export let plant;
  // ... resto del codice
</script>